import PageClient from './Client';

export const metadata = {
  title: { absolute: 'Wholesale Deal Presentation Template — Free | FreeDealCalc' },
  description: 'Free wholesale deal presentation template with verified market data — better than PowerPoint, generated automatically in 60 seconds.',
  alternates: { canonical: 'https://www.freedealcalc.com/tools/wholesale-deal-presentation-template' },
};

export default function Page() {
  return <PageClient />;
}
