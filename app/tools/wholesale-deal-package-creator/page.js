import PageClient from './Client';

export const metadata = {
  title: 'Wholesale Deal Package Creator — Free with Photos & Rentcast Comps | FreeDealCalc',
  description: 'Free wholesale deal package creator. Upload photos, pull Rentcast verified comps, add buyer spread calculation. Build a complete deal package in 60 seconds.',
  alternates: { canonical: 'https://www.freedealcalc.com/tools/wholesale-deal-package-creator' },
};

export default function Page() {
  return <PageClient />;
}
