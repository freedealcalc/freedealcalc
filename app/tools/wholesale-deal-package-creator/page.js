import PageClient from './Client';

export const metadata = {
  title: { absolute: 'Free Wholesale Deal Package Creator | FreeDealCalc' },
  description: 'Free wholesale deal package creator. Upload photos, pull verified comps, add buyer spread calculation, and build a deal package in 60 seconds.',
  alternates: { canonical: 'https://www.freedealcalc.com/tools/wholesale-deal-package-creator' },
};

export default function Page() {
  return <PageClient />;
}
